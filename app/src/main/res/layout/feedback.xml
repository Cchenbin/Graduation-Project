<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.specialty.administrator.me.FeedbackActivity">

    <include android:id="@+id/feedback_top"
             layout="@layout/top_all"/>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/feedback_top"
        android:padding="15dp"
        android:background="@color/colorLine"
        android:orientation="vertical">
        <EditText
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:background="@drawable/feedback"
            android:scrollHorizontally="false"
            android:layout_marginTop="10dp"
            android:hint="@string/feedback"
            android:gravity="top|start"
            android:textColor="@color/colorBlack"
            android:textColorHint="@color/colorLogisicsText"/>
        <Button
            android:id="@+id/back_submit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="@string/feedback_button"
            android:background="@drawable/button_back"
            android:textColor="@color/colorWhite"
            android:textSize="@dimen/text_size_m"/>
    </LinearLayout>
</RelativeLayout>
